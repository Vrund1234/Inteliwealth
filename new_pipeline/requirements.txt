# Pinned. python_scripts/requirements.txt lists 9 bare names with no versions.
pandas==2.3.3
numpy==2.2.6
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
openpyxl==3.1.5
# xlrd >= 2.0 reads legacy .xls only (2.x dropped .xlsx). All four WBR files are
# OLE2 BIFF .xls. Absent from python_scripts/venv, which is why we need our own.
xlrd==2.0.2
pytest==9.1.1
